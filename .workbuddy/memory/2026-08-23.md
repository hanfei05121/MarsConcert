# 2026-08-23

## 歌星视图「返回歌手列表」按钮样式优化
- `src/renderer/main/App.vue` 的 `.back-link`（歌星页「返回歌手列表」、歌单页「返回歌单」共用）由纯文字链接改为圆角胶囊按钮：
  - 默认 `bg-2` 底 + `line` 描边 + `text-1` 文字，轻微投影；
  - 悬停：`accent` 描边 + `accent-soft` 底色 + `text-0`，箭头图标左移 3px，整体符合 KTV 主题；
  - `:active` 缩放到 0.96 按下反馈；`.plhead .back-link` 去掉多余下边距。
- 注意冲突：全局 `style.css` 的 `button:hover{transform:translateY(2px)}` 仍生效（`.back-link:hover` 未覆盖 transform），与全站卡片/标签悬停一致，属预期。
- vue-tsc --noEmit 通过（exit 0）。

## 火星人专属主题（MARS EDITION）换皮
- 需求：把原「深紫蓝 KTV 风」整体换成华晨宇火星主题。IP 元素来自粉丝圈：火星人(代表色红)/ET、黑煤球(圆黑煤球+顶小红花，燃烧成火人)、Perper(花花黑猫)、Marsper(花花潮玩)、火星演唱会/火星乐园2.0。
- 设计方向：**暖炭黑底 + 火星红/熔岩橙炽光 + 星尘背景**（呼应「黑煤球燃成火人」+ 2026 火星乐园暗面）。
- 改动清单：
  - `src/renderer/main/style.css`：整套 :root token 改为火星色；body 加星尘(body::before 重复径向渐变) + 底部熔光；scrollbar hover 改暖色。
  - `src/renderer/assets/coal-ball.jpg`：手绘黑煤球吉祥物（已 cp 一份到项目根供预览用）。
  - `src/renderer/main/components/Sidebar.vue`：用户头像改为黑煤球(`url(../../assets/coal-ball.jpg)`，注意 components 下是 `../../`)。
  - `src/renderer/main/App.vue`：推荐页加火星人 Hero（import coalBall + 标题「火星人，欢迎回家 🔥」）。
  - `src/renderer/main/components/TopBar.vue`：加「火星点歌台」品牌点。
  - `src/renderer/player/components/LyricsOverlay.vue`：当前句高亮 + 倒计时格 粉(#ff5ca8)→火星炽红(#ff5a2e)。
  - `src/renderer/player/App.vue`：原唱徽标粉→炽红、伴奏徽标青→熔岩橙；空闲页冷蓝底→暖炭黑；idle 发光去粉。
  - `src/renderer/main/components/SongList.vue`：选中行粉紫渐变→火星红橙。
  - `src/main/windows.ts`：主窗口标题/底色改火星版；`index.html` title 改「火星点歌台 · MARS KTV」。
  - `mars-theme-preview.html`：独立预览页（复刻主界面+新主题+AI 火星背景图 `Dark_cosmic_Mars_concert_atmos_2026-08-23T12-46-54.png`），浏览器直接打开看效果。
- 配色速查：--bg-0 #14100e / --accent #ff4d2e / --accent-2 #ff8c42 / --accent-3 #ffd166(星金) / --on-accent #1c0d08。
- 注意：旧深紫蓝主题 token 已被整体覆盖（非增量），如需回退看 git 历史。
